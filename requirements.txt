# FC Global Group — Python Dependencies
# Document processing pipeline (markitdown + Claude analysis)
markitdown[all]>=0.1.0
anthropic>=0.40.0

# Optional: OpenAI SDK (only needed for LLM image descriptions in markitdown)
# openai>=1.0.0
